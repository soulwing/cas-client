<?xml version="1.0" encoding="UTF-8"?>
<project name="cas-client" default="compile" basedir=".">
  <property name="build.dir" location="build"/>
  <property name="build.prod.dir" location="${build.dir}/classes"/>
  <property name="build.test.dir" location="${build.dir}/test-classes"/>
  <property name="src.dir" location="src"/>
  <property name="test.dir" location="test"/>
  <property name="resources.dir" location="resources"/>
  <property name="build.lib.dir" location="vendor/lib"/>
  <property name="build.testlib.dir" location="vendor/test-lib"/>

  <path id="project.classpath">
    <pathelement location="${build.resources.dir}"/>
    <pathelement location="${build.prod.dir}"/>
    <pathelement location="${build.test.dir}"/>
    <fileset dir="${build.lib.dir}">
      <include name="*.jar"/>
    </fileset>
    <fileset dir="${build.testlib.dir}">
      <include name="*.jar"/>
    </fileset>
  </path>
  
  <target name="prepare">
    <mkdir dir="${build.prod.dir}"/>
    <mkdir dir="${build.test.dir}"/>
  </target>
  
  <target name="clean">
    <delete dir="${build.prod.dir}"/>
    <delete dir="${build.test.dir}"/>
  </target>
  
  <target name="compile" depends="compile.src, compile.test"/>
  
  <target name="test" depends="compile.test">
    <junit haltonfailure="true">
      <classpath refid="project.classpath"/>
      <formatter type="brief" usefile="false"/>
      <batchtest>
        <fileset dir="${build.test.dir}" includes="**/*Test.class"/> 
      </batchtest>
    </junit>  
  </target>
  
  <target name="compile.src" depends="prepare">
    <javac srcdir="${src.dir}" destdir="${build.prod.dir}" source="1.4"
           debug="true">
      <compilerarg value="-Xlint:all"/>
      <compilerarg value="-Xlint:-deprecation"/>
      <classpath refid="project.classpath"/>
    </javac>
  </target>
  
  <target name="compile.test" depends="prepare">
    <javac srcdir="${test.dir}" destdir="${build.test.dir}" source="1.4"
           debug="true">
      <compilerarg value="-Xlint:all"/>
      <compilerarg value="-Xlint:-deprecation"/>
      <classpath refid="project.classpath"/>
    </javac>
  </target>

</project>